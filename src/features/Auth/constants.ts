export const LS_USERNAME = "ICS_username";
export const LS_PASSWORD = "ICS_password";

export const username = localStorage.getItem(LS_USERNAME);
export const password = localStorage.getItem(LS_PASSWORD);
